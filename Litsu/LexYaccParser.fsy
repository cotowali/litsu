%{
open Litsu.SyntaxTree
%}

%start program

%token EOF
%token <int64> INT

%token PLUS
%left PLUS

%type <SyntaxTree.Node> program

%%

program:
  | expr EOF { Node.Expr $1 }

expr:
  | expr PLUS expr { Expr.Add($1, $3) }
  | INT { Expr.Int $1 }
